# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Home controller handles login and display of home dashboard
GET     /                           controllers.HomeController.index()
GET     /home                       controllers.HomeController.home()
POST    /validate                   controllers.HomeController.validate()
POST    /createuser                    controllers.HomeController.createUser()
GET     /logout                     controllers.HomeController.logout()
GET     /login                      controllers.HomeController.validate()


# Inventory controller handles all inventory management routes
GET     /ingredient                 controllers.InventoryController.addIngredient()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
